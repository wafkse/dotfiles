# Per-application Theme Overrides Schemas

This contains the sub-schemas for each theme override for a specific application.

This allows fine-grained control over how each application selects its specific theme colors in a per-application and per-theme manner, allowing for maximum flexibility.
