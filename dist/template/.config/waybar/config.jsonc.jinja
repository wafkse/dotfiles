{#- NOTE: Mirage-facing TOML uses kebab-case; translate Waybar's underscore-native keys. -#}
{%- if Waybar is defined -%}
{{ Waybar | tojson(indent=2)
    | replace('"reload-style-on-change":', '"reload_style_on_change":')
    | replace('"idle-inhibitor":', '"idle_inhibitor":') }}
{%- endif -%}
